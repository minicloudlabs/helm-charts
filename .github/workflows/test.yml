name: Test Workflow
on: [push]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Setup helm
      uses: azure/setup-helm@v3
      with: { version: v3.10.1 }
    - name: Lint
      run: make lint
